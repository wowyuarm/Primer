{
  "name": "primer",
  "type": "module",
  "version": "2.0.0",
  "description": "An Opinionated Anti-Library for AI-driven Design. Each Primer is a complete philosophical universe.",
  "scripts": {
    "dev": "npm run dev --prefix src",
    "build": "npm run build --prefix src",
    "preview": "npm run preview --prefix src"
  }
}
