---
/**
 * Primer Landing Page
 * 纯排版宣言页 — 无主题色，只有黑白与文字
 */

const primerSlugs = [
  'rams-60',
  'vignelli-canon',
  'hara-void',
  'ando-concrete', 
  'barragan-wall',
  'mondrian-neoplast',
  'memphis-81',
  'anderson-symmetry',
  'gatsby-deco',
  'neon-noir'
];
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Primer — An Opinionated Anti-Library for AI-driven Design. Code with Intention." />
  <title>Primer — Beyond the Gradient</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
</head>
<body>
  <main class="landing">
    <article class="manifesto">
      <header class="manifesto-header">
        <h1 class="title">Primer</h1>
        <p class="subtitle">Beyond the Gradient</p>
      </header>

      <section class="statement">
        <p class="lead">
          蓝紫渐变已成为平庸的代名词。<br/>
          Primer 是对审美惯性的无声抗议。
        </p>
      </section>

      <section class="principles">
        <div class="principle">
          <span class="number">01</span>
          <div class="content">
            <strong>叙事核心</strong>
            <span>灵魂从何而来</span>
          </div>
        </div>
        <div class="principle">
          <span class="number">02</span>
          <div class="content">
            <strong>系统规则</strong>
            <span>骨架如何支撑</span>
          </div>
        </div>
        <div class="principle">
          <span class="number">03</span>
          <div class="content">
            <strong>标志性约束</strong>
            <span>通过不做什么定义自己</span>
          </div>
        </div>
      </section>

      <section class="cta">
        <a href="/explore" class="explore-btn" id="exploreBtn">
          <span class="btn-text">Explore</span>
          <span class="btn-arrow">→</span>
        </a>
        <p class="hint">进入一个随机的设计宇宙</p>
      </section>

      <footer class="manifesto-footer">
        <p>Code with Intention.</p>
        <a href="/about" class="about-link">About</a>
      </footer>
    </article>
  </main>

  <script define:vars={{ primerSlugs }}>
    // 点击时随机选择一个 Primer
    document.getElementById('exploreBtn').addEventListener('click', (e) => {
      e.preventDefault();
      const randomIndex = Math.floor(Math.random() * primerSlugs.length);
      const randomSlug = primerSlugs[randomIndex];
      window.location.href = `/explore?primer=${randomSlug}`;
    });
  </script>
</body>
</html>

<style>
  /* === Reset === */
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  /* === Landing Page — Pure Typography, No Color === */
  html {
    font-size: 16px;
    -webkit-font-smoothing: antialiased;
  }

  body {
    min-height: 100vh;
    background: #FAFAFA;
    color: #1A1A1A;
    font-family: "Inter", system-ui, sans-serif;
  }

  .landing {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 48px 24px;
  }

  .manifesto {
    max-width: 560px;
    text-align: center;
  }

  /* === Header === */
  .manifesto-header {
    margin-bottom: 64px;
  }

  .title {
    font-size: 4rem;
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 12px;
  }

  .subtitle {
    font-size: 1rem;
    font-weight: 400;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.5;
  }

  /* === Statement === */
  .statement {
    margin-bottom: 64px;
  }

  .lead {
    font-size: 1.25rem;
    line-height: 1.8;
    opacity: 0.8;
  }

  /* === Principles === */
  .principles {
    display: flex;
    flex-direction: column;
    gap: 24px;
    margin-bottom: 80px;
    text-align: left;
  }

  .principle {
    display: flex;
    align-items: baseline;
    gap: 20px;
    padding-bottom: 24px;
    border-bottom: 1px solid rgba(0,0,0,0.1);
  }

  .principle:last-child {
    border-bottom: none;
  }

  .number {
    font-size: 0.75rem;
    font-weight: 500;
    opacity: 0.4;
    font-variant-numeric: tabular-nums;
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .content strong {
    font-size: 1rem;
    font-weight: 600;
  }

  .content span {
    font-size: 0.875rem;
    opacity: 0.6;
  }

  /* === CTA === */
  .cta {
    margin-bottom: 80px;
  }

  .explore-btn {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 16px 40px;
    background: #1A1A1A;
    color: #FAFAFA;
    font-size: 1rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .explore-btn:hover {
    gap: 20px;
    background: #000;
  }

  .btn-arrow {
    transition: transform 0.3s;
  }

  .explore-btn:hover .btn-arrow {
    transform: translateX(4px);
  }

  .hint {
    margin-top: 16px;
    font-size: 0.75rem;
    opacity: 0.4;
  }

  /* === Footer === */
  .manifesto-footer {
    font-size: 0.875rem;
    opacity: 0.3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }

  .about-link {
    color: inherit;
    text-decoration: none;
    opacity: 0.6;
    transition: opacity 0.2s;
  }
  .about-link:hover {
    opacity: 1;
  }

  /* === Responsive === */
  @media (max-width: 600px) {
    .title {
      font-size: 2.5rem;
    }
    .lead {
      font-size: 1.1rem;
    }
    .principles {
      gap: 20px;
    }
  }
</style>
